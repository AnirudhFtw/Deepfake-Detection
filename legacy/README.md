# Legacy scripts

These scripts are retained only as a record of the original single-branch
prototype. Do not use them for new experiments.

Use `pipeline/` to build datasets and `spatial/` or `frequency/` to train and
evaluate the current branches.
